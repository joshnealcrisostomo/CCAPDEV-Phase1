function deletePost() {
    const confirmation = confirm("Are you sure you want to delete this post?");
    if (confirmation) {
        // Add code here to delete the post, e.g., remove it from the DOM
        alert("Post deleted successfully!"); // Temporary feedback, replace with actual delete logic
    }
}

function deleteComment() {
    const confirmation = confirm("Are you sure you want to delete this comment?");
    if (confirmation) {
        // Add code here to delete the post, e.g., remove it from the DOM
        alert("Post deleted successfully!"); // Temporary feedback, replace with actual delete logic
    }
}